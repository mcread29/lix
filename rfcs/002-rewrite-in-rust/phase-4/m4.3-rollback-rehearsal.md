# M4.3 Rollback Rehearsal

Status: complete

## Scenario

- Start in `rust_active` mode.
- Revert to `legacy` mode and verify callback route is removed while SDK query behavior stays functional.

## Verification

- Existing boot tests assert:
  - `legacy` mode returns `LIX_CALL_UNKNOWN` for rust callback route.
  - `rust_active` mode enables callback execute route.

Commands:

- `pnpm --filter @lix-js/sdk exec vitest run src/engine/boot.test.ts`

Result: rollback behavior verified by test assertions.
